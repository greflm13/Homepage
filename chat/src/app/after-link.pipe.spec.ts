import { AfterLinkPipe } from './after-link.pipe';

describe('AfterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new AfterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
