import { BeforeLinkPipe } from './before-link.pipe';

describe('BeforeLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new BeforeLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
